import React from 'react';
import './Servicios.css';

export const Layout = ({ children }) => {
    return (
        <React.Fragment>
            {children} {}
        </React.Fragment>
    );
};

export default Layout;