
export const ContentPagesUser = ({ nav, main, footer, favoritess, transaccion, serUser }) => {
    return (
        <>
            <div>{nav}</div>
            <div>{main}</div>
            <div>{favoritess}</div>
            <div>{transaccion}</div>
            <div>{serUser}</div>
        </>
    )
}