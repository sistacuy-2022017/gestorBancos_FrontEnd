
export const ContentPagesUser = ({ nav, main, footer, favoritess, transaccion }) => {
    return (
        <>
            <div>{nav}</div>
            <div>{main}</div>
            <div>{favoritess}</div>
            <div>{transaccion}</div>
        </>
    )
}