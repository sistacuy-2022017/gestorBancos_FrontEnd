
export const ContentPagesUser = ({ nav, main, footer, favoritess }) => {
    return (
        <>
            <div>{nav}</div>
            <div>{main}</div>
            <div>{favoritess}</div>
        </>
    )
}