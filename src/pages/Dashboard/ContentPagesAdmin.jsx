export const ContentPagesAdmin = ({ navb, card, divi, serv, listA }) => {
    return (
        <>
            <div>{navb}</div>
            <div>{card}</div>
            <div>{divi}</div>
            <div>{serv}</div>
            <div>{listA}</div>
        </>
    )
}