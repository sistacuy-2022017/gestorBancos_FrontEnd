export const ContentPagesAdmin = ({ navb, card, divi, serv, tablesus }) => {
    return (
        <>
            <div>{navb}</div>
            <div>{card}</div>
            <div>{divi}</div>
            <div>{serv}</div>
            <div>{tablesus}</div>
        </>
    )
}