export const ContentPagesAdmin = ({ navb, divi, serv, tablesus }) => {
    return (
        <>
            <div>{navb}</div>
            <div>{divi}</div>
            <div>{serv}</div>
            <div>{tablesus}</div>
        </>
    )
}