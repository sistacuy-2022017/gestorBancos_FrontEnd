export const ContentPagesAdmin = ({ navb, card, divi, serv }) => {
    return (
        <>
            <div>{navb}</div>
            <div>{card}</div>
            <div>{divi}</div>
            <div>{serv}</div>
        </>
    )
}